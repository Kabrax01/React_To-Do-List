import React from "react";
import ReactDOM from "react-dom/client";
import "./ToDoApp/styles/styles.css";
import App from "./ToDoApp/App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
